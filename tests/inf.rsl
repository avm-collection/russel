proc (main) {
	(using_for)
}

proc (using_for)   for true;   (writef "Im infinite!\n" 1) {}
proc (using_while) while true  (writef "Im infinite!\n" 1)
proc (using_until) until false (writef "Im infinite!\n" 1)
